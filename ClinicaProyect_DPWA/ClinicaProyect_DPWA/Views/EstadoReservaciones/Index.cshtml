@model IEnumerable<ClinicaProyect_DPWA.Models.EstadoReservacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EstadoDeCita)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EstadoDeCita)
        </td>
        <td>
            @Html.ActionLink(" ", "Edit", new { id=item.Id_EstadoReservacion }, htmlAttributes: new { @class = "glyphicon glyphicon-pencil btn btn-success" }) |
            @Html.ActionLink(" ", "Details", new { id=item.Id_EstadoReservacion }, htmlAttributes: new { @class = "glyphicon glyphicon-eye-open btn btn-primary" }) |
            @Html.ActionLink(" ", "Delete", new { id=item.Id_EstadoReservacion }, htmlAttributes: new { @class = "glyphicon glyphicon-trash btn btn-danger" })
        </td>
    </tr>
}

</table>
